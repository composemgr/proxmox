# Proxmox VE in Docker — environment variable reference
# Copy relevant lines to a .env file alongside docker-compose.yaml
# NOTE: Requires KVM support (/dev/kvm) on the host.

# Timezone
TZ=America/New_York

# Hostname for the PDM container
BASE_HOST_NAME=pdm
